# PostgreSQL
# Part of STMNA_Desk — https://github.com/stmna-io/stmna-desk
# Base system setup: docs/install-guide.md | All stacks: stacks/README.md

x-podman:
  in_pod: false

services:
  postgres-voice:
    # INFO: PostgreSQL 17 for Signal pipeline queue and content cache
    image: docker.io/postgres:17
    container_name: postgres-voice
    restart: always
    ports:
      - "5432:5432"  # OPTIONAL: adjust to taste
    environment:
      POSTGRES_DB: stmna_voice  # REQUIRED: replace with your database name
      POSTGRES_USER: voice  # REQUIRED: replace with your database username
      POSTGRES_PASSWORD: YOUR_POSTGRES_PASSWORD  # REQUIRED: replace with a strong password
    volumes:
      - /home/YOUR_USER/data/postgres:/var/lib/postgresql/data  # REQUIRED: replace with your data path
    networks:
      - default
      - stmna-net

networks:
  default: {}
  stmna-net:
    external: true
